name: test

on: [push, pull_request]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv venv && source .venv/bin/activate && uv pip install -e ".[test]"
      - run: source .venv/bin/activate && pytest -v
